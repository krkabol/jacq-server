parameters:
    environment: 'development'

services:
    cacheStorage:
        factory: Nette\Caching\Storages\DevNullStorage

aws:
    endpoint: http://minio:9000
    credentials:
        key: minio
        secret: password
